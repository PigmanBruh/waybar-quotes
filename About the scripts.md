## How `quote_picker.sh` Works
- Remove comments and empty lines, then save it into a separate file
- Pick a random line from the processed entries and write it into `picked.txt`
- The waybar module read `picked.txt`
- The quote is displayed